## Аудиорефлексии (автоматически)

Запустить обработку аудиорефлексий (если есть новые записи):

```bash
bash extensions/process-reflections.sh
```

Если новых записей нет — скрипт тихо завершится. Результат не блокирует сессию.
