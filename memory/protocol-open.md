# Протокол Open (ОРЗ-фрактал)

> **Два масштаба:** День и Сессия. Триггер определяет масштаб.
> **Источник:** CLAUDE.md § 2 (slim) → этот файл.

---

## § Масштаб: День (Day Open)

> **Триггер:** «открывай» / «открывай день» / «открой день»
> **Роль:** R1 Стратег
> **Два выхода:** (1) **Day Open в VS Code** — compact dashboard (20-30 строк), только метрики + план + блокеры. (2) **DayPlan в git** — полный отчёт со всеми секциями (80+ строк, collapsible).
> **Handoff:** DayPlan = Стратег→Человек handoff artifact (PROCESSES.md §3.2). Задел из вчерашнего Day Close = Agent→Agent handoff (вечерний→утренний Стратег).
>
> ### Различение: Day Open (VS Code) vs DayPlan (git)
>
> | | VS Code (Day Open) | Git (DayPlan) |
> |---|---|---|
> | **Для кого** | Человек прямо сейчас | Архив + детальная сверка |
> | **Объём** | 20-30 строк | 80+ строк (collapsible) |
> | **Содержит** | Вчера (1 строка), бот QA (1 строка), календарь (кратко), бюджет, план (таблица без контекста), блокеры | Всё: контекст, ссылки, видео, контент, мир, QA подробно |
> | **Бот QA** | 1 строка: дельта + urgent count | Полная секция: таблица urgent, кластеры |
> | **Ссылка** | → на DayPlan в git | — |
>
> **Принцип:** VS Code = быстрый старт. Git = подробности при необходимости.
>
> **Режим вывода (day-rhythm-config.yaml → `day_open.interactive`):**
> - `interactive: false` (по умолчанию) → Day Open выводится **одним блоком** без промежуточных вопросов. Все пункты, требующие решения, собираются в секцию **«Требует внимания»** в конце отчёта. Нечего решать → Day Open завершается без вопросов.
> - `interactive: true` → поэтапный вывод с вопросами после каждого шага (помидорки, видео и т.д.).

### Общий принцип: синхронизация результатов месяца

> **Source-of-truth:** `Strategy.md § Результаты месяца` (R1-R{N}). Направление: Strategy.md → WeekPlan → DayPlan → compact dashboard.
> При создании/обновлении WeekPlan — **сначала прочитать Strategy.md**, затем перенести формулировки R1-R{N} дословно. НЕ изобретать свои названия результатов.
> При изменении результата (переформулировка, новый R{N}) — обновить Strategy.md **первым**, затем WeekPlan и ревью-таблицы.
> Колонка «Результат» в таблице РП = краткое название из Strategy.md (не ID). РП без привязки → «—».

### Общий принцип: фильтр свежести (Day Open + Session-Prep)

> **Issues, видео, заметки:** В Day Open и Session-Prep показывать только данные за последние **2 дня**. Старые issues, необработанные видео >2 дней, архивные заметки — не выводить (они отслеживаются отдельно в Strategy Session / backlog). Исключение: urgent / blocking items показывать всегда.

### Алгоритм Day Open

#### 0. Дата и контекст (БЛОКИРУЮЩЕЕ — до любых выводов)

> **ПЕРВОЕ действие.** Выполнить ДО чтения конфигов, ДО выводов о дне недели, ДО strategy_day проверки. `currentDate` от Anthropic ВРЁТ (timezone).

```bash
date "+%Y-%m-%d %A %H:%M %Z"
```

Только после получения результата определять:
- День недели → strategy_day?
- Дата → для коммитов, фильтров, DayPlan

**SchedulerReport:** читать лог за СЕГОДНЯ (`~/logs/strategist/YYYY-MM-DD.log`), НЕ файл из `current/SchedulerReport*.md` (может быть вчерашний). Если лога нет → «scheduler ещё не запускался сегодня».

#### 1. Вчера

Собрать коммиты за вчера по всем репо:

```bash
for repo in $(ls ~/IWE/); do
  if [ -d ~/IWE/$repo/.git ]; then
    commits=$(git -C ~/IWE/$repo log --since="yesterday 00:00" --until="today 00:00" --oneline --no-merges 2>/dev/null)
    [ -n "$commits" ] && echo "=== $repo ===" && echo "$commits"
  fi
done
```

Сопоставить с DayPlan вчера → вывести 1-3 ключевых результата.

#### 1b. GitHub Issues (сбор по репо)

Собрать открытые issues из всех репо workspace:

```bash
# Собираем owner/repo из git remote origin
for repo_dir in ~/IWE/*/; do
  [ -d "$repo_dir/.git" ] || continue
  remote=$(git -C "$repo_dir" remote get-url origin 2>/dev/null) || continue
  # Извлекаем owner/repo из URL (https или ssh)
  slug=$(echo "$remote" | sed -E 's#.*github\.com[:/]##; s#\.git$##')
  issues=$(gh issue list --repo "$slug" --state open --json number,title,labels,createdAt --jq '.[] | "#\(.number) \(.title) [\(.labels | map(.name) | join(", "))] (\(.createdAt[:10]))"' 2>/dev/null)
  [ -n "$issues" ] && echo "=== $(basename "$repo_dir") ===" && echo "$issues"
done
# DS-IT-systems — вложенные репо
for repo_dir in ~/IWE/DS-IT-systems/*/; do
  [ -d "$repo_dir/.git" ] || continue
  remote=$(git -C "$repo_dir" remote get-url origin 2>/dev/null) || continue
  slug=$(echo "$remote" | sed -E 's#.*github\.com[:/]##; s#\.git$##')
  issues=$(gh issue list --repo "$slug" --state open --json number,title,labels,createdAt --jq '.[] | "#\(.number) \(.title) [\(.labels | map(.name) | join(", "))] (\(.createdAt[:10]))"' 2>/dev/null)
  [ -n "$issues" ] && echo "=== DS-IT-systems/$(basename "$repo_dir") ===" && echo "$issues"
done
```

- Показать сводную таблицу (если есть issues):

```markdown
## GitHub Issues (открытые)

| Репо | # | Название | Метки | Создано | Связь с РП |
|------|---|----------|-------|---------|------------|
| repo | #N | title | labels | YYYY-MM-DD | WP-N / — |

**Всего:** N issues в M репо
```

- Связь с РП: сопоставить название issue с РП из MEMORY.md (по ключевым словам). Нет совпадений → «—»
- Новые issues (за последние 24ч) → добавить в «Требует внимания»
- **Фильтр свежести:** Показывать только issues, созданные за последние 2 дня. Старые issues не показывать — они уже известны и отслеживаются отдельно.
- Если issues нет → *«GitHub Issues: открытых нет.»* (1 строка, секция не выводится)
- **В VS Code (compact):** 1 строка: `**Issues:** N новых за 2 дня в M репо [#X repo, #Y repo]`

#### 1c. Разбор заметок (Note-Review)

- Прочитать `DS-my-strategy/inbox/fleeting-notes.md`
- Для каждой **новой** заметки (с прошлого Day Open) определить категорию и предложить действие:

| Категория | Действие |
|-----------|----------|
| **→ РП** | Предложить формулировку РП (артефакт + критерий). Если одобрено — через WP Gate |
| **→ Backlog** | Добавить в WP-context file существующего РП (указать какого) |
| **→ Контент** | Добавить в draft-list.md или backlog контент-плана |
| **→ Pack** | Различение/метод/паттерн → capture-candidate для Экстрактора |
| **→ Обсудить** | Неясно — вынести в «Требует внимания» |
| **→ Шум** | Зачеркнуть (`~~текст~~`) |

- Формат секции в DayPlan:

```markdown
## Заметки (разбор)

> Источник: `DS-my-strategy/inbox/fleeting-notes.md` | Новых: N (с DD мес)

| # | Заметка | Дата | Категория | Предложение |
|---|---------|------|-----------|-------------|
| 1 | [краткое содержание] | DD мес | → РП / → Backlog / ... | [конкретное действие] |

**Итого:** N новых, M → РП, K → backlog, J → шум
```

- Старые заметки с 🔄 — повторно показать с рекомендацией
- **НЕ удалять заметки из fleeting-notes.md** при Day Open. Обработанные помечаются обычным шрифтом (снимается bold). Удаление — только при Day Close после согласования
- Если заметок нет → *«Заметки: новых нет.»* (1 строка, секция не выводится)
- **В VS Code (compact):** 1 строка: `**Заметки:** N новых [M → РП, K → backlog]`

#### 2. План на сегодня

- Прочитать текущий `WeekPlan W{N}...` из `DS-my-strategy/current/`
- **Linear check:** Запустить `linear-sync.sh --list` → сверить с MEMORY.md. Новые назначения (issues, которых нет в WP-REGISTRY) → провести через WP Gate. Изменённые статусы → обновить MEMORY.md (git = source of truth, но Linear может содержать свежие назначения от других участников)
- Выбрать 2-4 РП на сегодня
- **Правило слота 1 (ОБЯЗАТЕЛЬНО):** Первый РП в таблице — ВСЕГДА саморазвитие. Содержит: заметки, черновики или заготовки по изучаемым темам, которые пользователь должен написать сам. Источники: текущее руководство, активные курсы, WP с тегом саморазвитие/учёба/курс.

#### 3. Саморазвитие

- Показать текущее руководство (из `DS-principles-curriculum/` или контекст из WeekPlan)
- Где остановился (из WP context file)
- Список активных черновиков: `ls DS-my-strategy/drafts/` (≤5 = норма)

#### 4. Стратегирование (⚠️ может быть блокирующим)

- Прочитать `memory/day-rhythm-config.yaml` → `day_open.strategy_day`
- Если сегодня = `strategy_day`:
  - **DayPlan НЕ создавать.** План на понедельник уже встроен в WeekPlan (секция «План на понедельник», создаётся session-prep).
  - Показать: *«Сегодня — день стратегии. План дня = WeekPlan → секция «План на понедельник». DayPlan не создаётся.»*
  - Показать текущий WeekPlan из `DS-my-strategy/current/`
  - **Пропустить шаг 7** (Запись DayPlan) — сразу закончить Day Open

#### 4b. Ритм дня (помидорки)

- Прочитать `memory/day-rhythm-config.yaml` → секция `pomodoro`
- Показать текущие настройки: *«Помидорки: {work_minutes}/{break_minutes}, длинный перерыв {long_break_minutes} мин после {sessions_before_long_break} помидорок. Алерт при >{session_alert_minutes} мин.»*
- **Если `interactive: true`:** *«Скорректировать? (например: "поставь 30/10")»*
- **Если `interactive: false`:** показать настройки молча. Если пользователь захочет изменить — сделает это отдельной командой.

#### 4c. Календарь дня

- Вызвать `mcp__google-calendar__list-calendars` → собрать ВСЕ calendarId
- Вызвать `mcp__google-calendar__list-events` (calendarId=[все календари], timeMin/timeMax=сегодня, timeZone из аккаунта)
- **Время:** Всегда показывать в локальном timezone пользователя (из `get-current-time`). Если событие хранится в другом timezone — пересчитать
- Показать таблицу событий:

```markdown
## Календарь (DD месяца)

| Время | Событие | Длит. | Связь с РП |
|-------|---------|-------|------------|
| HH:MM | Название | Xh/Xмин | WP-N / — |

⏱ Свободных блоков ≥1h: [слоты]
```

- Связь с РП: сопоставить название события с РП из MEMORY.md (по ключевым словам). Нет совпадений → «—»
- Свободные блоки: рассчитать промежутки ≥1h между событиями (рабочий день 09:00–19:00)
- Если событий нет → *«Календарь: событий на сегодня нет. Весь день свободен.»*
- Конфиденциальные события (visibility=private/confidential): пропустить (не показывать)

#### 5. IWE за ночь (светофор)

- Проверить логи автоматики (launchd, cron-скрипты):
  - sync-agent, note-review, reindex — отработали?
  - Ошибки?
- Новые коммиты от автоматики (git log --since="yesterday 18:00")
- Формат отображения — таблица-светофор:

```markdown
## IWE за ночь (светофор)

| Подсистема | Статус | Детали |
|------------|--------|--------|
| Scheduler | :green_circle: / :yellow_circle: / :red_circle: | [что отработало/упало] |
| update.sh | :green_circle: / :red_circle: | [drift report: `update.sh --check`] |
| template-sync | :green_circle: / :red_circle: | [статус] |
| MCP reindex | :green_circle: / :red_circle: | [статус] |
| Scout | :green_circle: / :yellow_circle: / :red_circle: | [N находок / fallback / не запустился] |

**Вывод:** [1-2 предложения — общая оценка + действия по красным]
```

- :green_circle: = всё ок, :yellow_circle: = предупреждение (некритично), :red_circle: = сбой (требует действия)
- Каждый красный → привязать к РП (обычно WP-7 техдолг)

#### 5b. Здоровье бота (QA)

- Прочитать `DS-my-strategy/inbox/unsatisfied-questions.md`
- Извлечь: дельту (сегодня vs вчера, 7д vs пред. 7д), количество urgent (status=new), топ-кластеры
- **Проверить дату отчёта.** Если дата файла < сегодня → дельту «vs вчера» не копировать (она устарела). Указать: «данные за [дата отчёта], свежий отчёт за [сегодня] не сгенерирован»
- **В VS Code (compact):** 1 строка: `**Бот:** N новых жалоб (↑↓X), M urgent открыто (самые старые ≤дата)`
- **В DayPlan (git):** полная секция «Здоровье бота (QA)» — дельта, таблица urgent, кластеры
- Если urgent count > 0 и есть новые (за последние 24ч) → добавить в «Требует внимания»
- Если отчёт не найден → `*Отчёт QA: файл не найден. Проверить unsatisfied-report.sh.*`

#### 5c. Контент (что можно опубликовать сегодня)

- Прочитать backlog из [Стратегии маркетинга §7](../DS-ecosystem-development/B.Aisystant-Ecosystem/B1.Society/B1.1.Meaning/1.1.2.%20Marketing/Стратегия%20маркетинга%201.1.md)
- Прочитать `DS-my-strategy/drafts/draft-list.md` → свежие Content ideas
- Выбрать 1-3 темы, которые можно адаптировать сегодня (исходя из свободных блоков и запланированных РП)
- Показать таблицу с рекомендацией

#### 5d. Scout (ночной разведчик)

- Проверить наличие единого отчёта: `DS-agent-workspace/scout/results/YYYY/MM/DD/report.md`
- Если отчёт есть:
  - Прочитать `report.md` → сводка + находки + captures + WP-предложения
  - Проверить статус ревью (шапка: «⬜ не проверен» / «✅ проверен»)
  - Показать сводку:

```markdown
## Scout (ночной разведчик)

**Запуск:** [дата], модель: [модель], бюджет: $[X]
**Находок:** N | **Capture-кандидатов:** M | **WP-предложений:** K
**Статус ревью:** ⬜ не проверен / ✅ проверен

### ТОП находки
| # | Находка | Релевантность | Связь с РП |
|---|---------|---------------|------------|
| 1 | [название] | высокая/средняя | WP-N |

*Отчёт: [DS-agent-workspace/scout/results/YYYY/MM/DD/report.md]*
```

- Если отчёт не проревьюен → добавить в «Требует внимания»: «Scout: отчёт за [дата] ждёт ревью»
- Если отчёта нет → `*Scout: не запускался или результатов нет.*`
- **Светофор:** Добавить строку Scout в таблицу IWE за ночь:
  - :green_circle: = запустился, есть находки
  - :yellow_circle: = запустился, но 0 находок или парсер сработал в fallback
  - :red_circle: = не запустился или ошибка

#### 6. Мир

- Прочитать `memory/day-rhythm-config.yaml` → секция `news`
- Для каждого топика из `topics`: если `feeds` заданы → RSS, иначе → WebSearch по `query`
- Лимит: `max_items_per_topic` новостей на топик
- Если `enabled: false` → секцию не включать
- **Обязательно:** каждая новость со ссылкой на источник: `[название](URL)`

#### 6b. Требует внимания (только при `interactive: false`)

> Собрать ВСЕ пункты, требующие решения пользователя, из шагов 1–6. Формат:
>
> ```markdown
> ## Требует внимания
>
> 1. [пункт — что решить]
> 2. ...
>
> *Нет пунктов → секция не выводится, Day Open завершается молча.*
> ```
>
> Примеры пунктов: отсутствие саморазвития в плане, ошибки автоматики (красные в светофоре), расхождения в статусах РП.

#### 7. Запись (два выхода)

- **Если strategy_day (шаг 4) → ПРОПУСТИТЬ этот шаг.** DayPlan на день стратегирования не создаётся.

**Порядок: сначала DayPlan (git), потом compact dashboard (VS Code).** Не выдавать compact dashboard до полной записи DayPlan — иначе пользователь получит неполные/ошибочные данные, которые потом придётся патчить.

**Выход 1 — DayPlan (git, полный отчёт):**
- Обновить или создать `DS-my-strategy/current/DayPlan YYYY-MM-DD.md` по шаблону DayPlan
- Если предыдущий DayPlan → `git mv → archive/day-plans/`
- Закоммитить в DS-my-strategy

**Выход 2 — VS Code (compact dashboard):**
- Показать в чат по шаблону «Day Open (VS Code — compact dashboard)»
- Содержит: вчера (1 строка), бот QA (1 строка), календарь (кратко), бюджет, план (таблица без колонки «Контекст»), блокеры
- Заканчивается ссылкой на DayPlan в git

### Шаблон DayPlan

> **Стиль:** collapsible, без `---`, `<b>` в summary. Приоритет = светофор (🔴🟡🟢). Результат = краткое название из Strategy.md.

```markdown
---
type: daily-plan
date: YYYY-MM-DD
week: W{N}
status: active
agent: Стратег
---

# Day Plan: DD месяца YYYY (День недели)

<details open>
<summary><b>План на сегодня</b></summary>

| 🚦 | # | РП | h | Статус | Результат |
|----|---|-----|---|--------|-----------|
| ⚫ | N | **Саморазвитие** — [тема] | 1-2 | pending | — |
| 🔴 | ... | **Название** | X | in_progress | Краткое название |

**Бюджет дня:** ~Yh РП всего / ~Xh физ / Плановый мультипликатор ~N.Nx

</details>
<details>
<summary><b>Календарь (DD месяца)</b></summary>

| Время | Событие | Длит. | Связь с РП |
|-------|---------|-------|------------|
| HH:MM | Название | Xh | WP-N / — |

⏱ Свободных блоков ≥1h: [слоты]

</details>
<details>
<summary><b>Здоровье бота (QA)</b></summary>

**Дельта:** Сегодня: N (↑↓X vs вчера) | За 7д: N (↑↓X vs пред. 7д)

| # | Вопрос | Sev | Cluster | Дата |
|---|--------|-----|---------|------|

</details>
<details>
<summary><b>IWE за ночь (светофор)</b></summary>

| Подсистема | Статус | Детали |
|------------|--------|--------|
| Scheduler | 🟢/🟡/🔴 | [детали] |
| template-sync | 🟢/🔴 | [статус] |
| Scout | 🟢/🟡/🔴 | [N находок] |

</details>
<details>
<summary><b>Наработки Scout (разбор)</b></summary>

> [Отчёт за DD мес](../../../DS-agent-workspace/scout/results/YYYY/MM/DD/report.md) — N находок, M capture-кандидатов
> **Статус ревью:** ⬜ не проверен / ✅ проверен
> **Конверсия в Pack:** [analytics.md](../../../DS-agent-workspace/scout/analytics.md) — X записей из Y captures (Z%)

**Ожидают разбора:** N captures за последние DD дней (без ревью).
Если N > 0 → добавить WP-170 (Разбор наработок Scout) в план дня.

</details>
<details>
<summary><b>Итоги вчера (DD мес)</b></summary>

**Коммиты:** N в M репо | **РП закрыто:** N

</details>

*Создан: YYYY-MM-DD (Day Open)*
```

### Шаблон Day Open (VS Code — compact dashboard)

> Это то, что Claude Code показывает в чат VS Code. Короткая сводка для быстрого старта. Подробности — в DayPlan (git).

```markdown
## DD месяца YYYY (День недели) — Day Open

**Вчера:** N РП done, N коммитов. Ключевое: #X [название], #Y [название]
**Issues:** N открытых в M репо [самые свежие: #X repo, #Y repo]
**Бот:** N новых жалоб (↑↓X vs вчера), M urgent открыто (самые старые ≤дата)
**Календарь:** [события через запятую или «свободен»]
**Бюджет:** W{N}: ~Zh РП / ~Yh физ | Сегодня: ~Zh РП / ~Yh физ
**ТОС:** [узкое горлышко из Strategy.md → фокус месяца → ТОС-месяца] | Прогресс: R1 ✅/🔄/⏳, R2 ...

### План дня

| 🚦 | # | РП | h |
|----|---|-----|---|
| ⚫ | N | **Саморазвитие** — [тема] | 1 |
| 🔴 | ... | **Название** | X |

### Требует внимания

1. [пункт — если есть]

*Нет пунктов → секция не выводится.*

> Подробно: [DayPlan YYYY-MM-DD](ссылка на git)
```

### Шаблон WeekPlan

> **Стиль:** collapsible, без `---`, `<b>` в summary. Приоритет = светофор (🔴🟡🟢). Результат = краткое название из Strategy.md (не ID).
> **Бюджет:** формат `~Zh РП всего (в том числе Xh на R1-R{N}) / ~Yh физ / Плановый мультипликатор ~N.Nx`. Активные — **жирным**. Done — зачёркнуть: `| ~~#~~ | ~~название~~ | ... |`

```markdown
---
type: week-plan
week: W{N}
date_start: YYYY-MM-DD
date_end: YYYY-MM-DD
status: draft
agent: Стратег
---

# WeekPlan W{N}: DD мес — DD мес YYYY

<details open>
<summary><b>План на неделю W{N}</b></summary>

**Фокус:** [1 предложение]
**Бюджет:** ~Zh РП всего (в том числе Xh на R1-R{N}) / ~Yh физ / Плановый мультипликатор ~N.Nx

> 🔴 критический 🟡 средний 🟢 низкий

| 🚦 | # | РП | h | Статус | Результат |
|----|---|-----|---|--------|-----------|
| 🔴 | ... | **Название** — описание | X | in_progress | Краткое название |

**Сводка:** N РП, Xh. 🔴 Xh (N) 🟡 Xh (N) 🟢 Xh (N)
**Off-plan:** [список]

</details>
<details>
<summary><b>Итоги прошлой недели W{N-1}</b></summary>

**Выполнение:** X/Y РП (Z%)
**Перенос:** [список]
**Ключевые выводы:** [2-3 пункта]

</details>
<details>
<summary><b>Стратегическая сверка</b></summary>

| ID | Результат | Бюджет | Статус | Связанные РП |
|----|-----------|--------|--------|-------------|
| R1 | ... | ... | ... | WP-X, WP-Y |

**ТОС-месяца:** [узкое горлышко]
**Расхождения:** [план vs факт]

**Scout за неделю:** N запусков, M находок, K captures. Конверсия в Pack: X%. Ожидают разбора: Y captures.

</details>
<details>
<summary><b>Повестка сессии стратегирования</b></summary>

- [ ] Ревью прошлой недели
- [ ] Inbox Triage (недельный)
- [ ] НЭП: проверить O1-O{N}
- [ ] Стратегическая сверка
- [ ] Scout: ревью наработок за неделю (analytics.md)
- [ ] Видео-ревью (С3)
- [ ] Проверка активации + ревью спящих

### Вопросы для обсуждения
1. ...

</details>
<details>
<summary><b>📋 Inbox Triage (недельный)</b></summary>

[из fleeting-notes, unsatisfied-questions, WP context files]

</details>
<details>
<summary><b>Контент-план W{N}</b></summary>

1. **#NNN Название** — канал, бюджет, дедлайн

</details>
<details open>
<summary><b>План на понедельник DD мес</b></summary>

| 🚦 | # | РП | h | Результат |
|----|---|-----|---|-----------|

</details>

*Создан: YYYY-MM-DD (Strategy Session)*
```

---

## § Масштаб: Сессия (Session Open)

> **Триггер:** Любое задание от пользователя (кроме триггеров Day Open/Close).
> **Роль:** R6 Кодировщик
> **Handoff:** WP context file = Human→Agent handoff artifact (PROCESSES.md §3.3). Секция «Осталось» = Agent→Agent handoff из предыдущей сессии.

### Различение: Сессия ≠ День

| | День | Сессия |
|---|------|--------|
| **Open** | Только триггер («открывай день»), без задания | Всегда с конкретным заданием от пользователя |
| **Close** | Отдельная сессия-ритуал (только триггер) | Внутри текущей сессии, завершение работы |

> Day Open и Day Close — **отдельные сессии**, состоящие только из триггера. Session Open/Close — всегда в контексте конкретной работы.

## WP Gate (блокирующее)

**ОБЯЗАТЕЛЬНО. ДО ЛЮБОГО ДЕЙСТВИЯ по задаче.**

1. Прочитать MEMORY.md → секция «РП текущей недели»
2. Совпадает с РП? → Да: п. 3. Нет: СТОП.

**ЗАПРЕЩЕНО до проверки:** читать код, исследовать, планировать, вызывать агентов.

### Совпадает — работаем

1. Ссылаемся на номер РП.
2. **DayPlan Gate:** Проверить `DS-my-strategy/current/DayPlan YYYY-MM-DD.md`. Если РП **нет в DayPlan** текущего дня → добавить строку в таблицу. **Исключение:** если сегодня strategy_day (понедельник) — DayPlan отсутствует (план в WeekPlan → «План на понедельник»). В этом случае Gate пропускается.
3. Переходим к Ритуалу.

### Не совпадает — СТОП

1. «Этой задачи нет в плане на неделю.»
2. Вывести таблицу РП из MEMORY.md
3. Спросить: артефакт, формулировка РП, репо, бюджет
4. Предложить перестановку РП если бюджет ограничен
5. Дождаться подтверждения
6. Записать **в пять мест** (атомарно):
   - `MEMORY.md` → таблица РП (новая строка)
   - `DS-my-strategy/docs/WP-REGISTRY.md` → новая строка в таблицу (сортировка: от последнего к первому)
   - `DS-my-strategy/current/WeekPlan W{N}...` → таблица РП (новая строка)
   - `DS-my-strategy/inbox/WP-{N}-{slug}.md` → context file (всегда: описание, источник, контекст, критерий готовности, `verification_class: trivial | closed-loop | open-loop | problem-framing`)
   - **Linear** → `mcp__linear__create_issue` (teamId: IWEMIM, приоритет по уровню РП). Git = source of truth, Linear = visual frontend
   > **Нумерация:** `{N}` — только последовательные целые числа (74, 75…). Буквенные суффиксы (73a, 73b) запрещены — каждый РП самостоятелен.
7. Только после записи во все пять — начать

### Исключения (РП не нужен)

- Задачи ≤15 мин
- Вопросы/исследования без изменений файлов
- Экстренные баг-фиксы (пользователь говорит «срочно»)

**Эскалация:** Если сессия начата по исключению, но пользователь задаёт второе задание → *«Это перерастает в РП. Записать?»*

> **Урок WP Gate (2026-02-09):** Переход от исследования к действию = переход через WP Gate. Даже если исследование было исключением, момент «теперь исправь» требует полной проверки. Никогда не квалифицировать задачу как исключение самостоятельно, если результат — файлы.

---

## Ритуал согласования

> После WP Gate, перед работой.

**Шаг 1.** Определить роль пользователя и объявить:
> **Роль пользователя:** [предложение из 4 ролей — memory/roles.md]
> **Роль Claude:** [из каталога DP.ROLE.001 §3.2]
> **Работа:** [что]
> **РП:** [артефакт]
> **Класс верификации:** [closed-loop / open-loop / problem-framing]
> **Метод:** [как]
> **Оценка:** ~Xh
> **Модель:** [текущая] — рекомендую [модель] ([причина])

> **Роль пользователя:** Предложить, из какой из 4 ролей Tseren даёт задание (HD #31): (1) Разработчик платформы, (2) Пользователь платформы, (3) Разработчик своего IWE, (4) Пользователь своего IWE. НЕ угадывать молча — всегда предложить и дождаться подтверждения.
> Роли Claude нет в каталоге → *«Роль не найдена. Предлагаю добавить: [название].»*

**Шаг 1b. Класс верификации** (HD #32):
> Определить verification_class задачи. Тест: «Сколько стоит проверить результат?»

| Класс | Проверка | Режим работы | Модель |
|-------|----------|-------------|--------|
| **trivial** | Не нужна (результат очевиден) | Агент автономно. Без Captures | Haiku |
| **closed-loop** | Дешёвая, автоматическая (тесты) | Агент автономно. Captures не обязательны | Sonnet |
| **open-loop** | Дорогая, отложенная | Совместно. Captures обязательны на рубежах | Opus |
| **problem-framing** | Неизвестная | Экзоскелетный: вопросы > ответы. Claude задаёт 3 уточняющих вопроса ДО предложения решения | Opus |

> Класс влияет на рекомендацию модели и режим работы в сессии.
>
> **Переключение модели — два сценария:**
>
> **Сценарий A. Вся сессия на другой модели** (определяется при Open):
> Если при открытии Claude определил, что вся сессия trivial/closed-loop и текущая модель избыточна — Claude говорит:
> *«Эта задача [trivial/closed-loop], рекомендую переключиться на [Haiku/Sonnet] через `/model`. Переключить автоматически я не могу.»*
> Пользователь сам переключает через `/model` → вся сессия идёт на дешёвой модели.
>
> **Сценарий B. Отдельная задача внутри сессии** (делегирование sub-agent'у):
> Посреди сессии появляется trivial-задача → Claude делегирует sub-agent'у на дешёвой модели. Основная сессия не прерывается. Делегирование возможно **только вниз**:
>
> | Основная сессия | Делегирование вниз (sub-agents) | Переключение вверх |
> |---|---|---|
> | **Opus** | → Sonnet, Haiku | — |
> | **Sonnet** | → Haiku | → Opus: только `/model` (пользователь) |
> | **Haiku** | — | → Sonnet/Opus: только `/model` (пользователь) |
>
> **Ограничение:** Claude Code не может переключить модель автоматически. Переключение вверх или смена модели для всей сессии — только пользователь через `/model`.

**Шаг 1c. Экзоскелетный режим** (только при problem-framing):
> **Принцип:** Экзоскелет усиливает мышление, не заменяет (HD #28, принцип #21).
> Claude НЕ предлагает решение сразу. Вместо этого:
> 1. Задать 3 уточняющих вопроса, помогающих пользователю структурировать задачу:
>    - **Что:** Какой артефакт/состояние должно получиться?
>    - **Зачем:** Какую проблему решаем? Что будет, если не решить?
>    - **Ограничения:** Что точно НЕ должно измениться? Какие границы?
> 2. Дождаться ответов.
> 3. Предложить 2-3 варианта подхода (не решения) с trade-offs.
> 4. Пользователь выбирает → переход к работе.
>
> Применяется на масштабе Сессия. На масштабе День: Day Open уже экзоскелетный по дизайну (Стратег предлагает, человек решает).

**Рекомендация модели** (с учётом verification_class):
- Opus — open-loop, problem-framing, архитектура, multi-system, стратегия
- Sonnet — closed-loop, типовые задачи, одно-файловые правки, контент
- Haiku — trivial (переименование, обновление ссылок, форматирование, поиск файла), cron-агенты, sub-agents
- Если рекомендуемая модель ≠ текущая:
  - Вся сессия на другой модели → *«Рекомендую переключиться на [модель] через `/model`. Переключить автоматически я не могу.»*
  - Отдельная задача внутри сессии → делегировать sub-agent'у (только вниз)

**Шаг 2.** Дождаться согласования (включая модель).

**Шаг 3.** Определить файлы/репо, ограничения. Если есть context file (`DS-my-strategy/inbox/WP-{N}*.md`) — прочитать.

> **Критичное чтение:** Context file может устареть. Иерархия доверия: (1) реальные файлы/код → (2) свежие документы → (3) WP context / WORKPLAN.

**Исключения:** Задачи §2.4 освобождены от WP Gate, но Ритуал (объявление) — всегда. Продолжение работы над тем же РП — повторный Ритуал не нужен.

**Шаг 4. Регистрация сессии** (после согласования):
> Добавить строку в `DS-my-strategy/inbox/open-sessions.log`:
> ```
> YYYY-MM-DD HH:MM | WP-N | модель | краткое описание задачи
> ```
> Если файл не существует — создать. Исключения (≤15 мин) — не регистрировать.

---

## Зонтичные РП (Umbrella WP)

Для систем с непрерывной работой — два постоянных РП:
- **#N-dev** (развитие) — фичи из MAPSTRATEGIC
- **#N-debt** (техдолг) — баги, latency, cleanup

Context file = живой backlog (10-20 пунктов). Done удаляются. Каждую неделю — конкретный scope в WeekPlan.

**Отсечка:** задача ≥4h или отдельный lifecycle → эпизодический РП.

---

## Issue Funnel (Воронка → WP-debt) — v2 DB-native

**Двухуровневый триаж (R7 Триажёр):**
- **Auto-triage (Grade 1):** helpful=false / comment → Haiku classify → `feedback_triage` DB → TG alert if severity>=high
- **Review (Grade 3):** при открытии сессии техдолга → structured report + 2 файловых intake

**Источники:** `feedback_triage` DB, `unsatisfied-questions.md` (REPORT, не inbox), `fleeting-notes.md`, `captures.md`.

**Review-протокол (при открытии сессии техдолга):**
1. Прочитать unsatisfied-questions.md
2. Прочитать fleeting-notes + captures
3. Прочитать WP-debt backlog
4. Review + оценить бюджет
5. Одобренные → backlog, отработанные → `status='resolved'`

**Прямая команда:** «запиши замечание: X» → fleeting-notes.md с тегом бот.

---

## Обслуживание экзокортекса (из claude-md-maintenance.md)

| Слой | Файл | Лимит | Загрузка |
|------|------|-------|----------|
| 1 | MEMORY.md | ≤100 строк | Авто (всегда) |
| 2 | ~/IWE/CLAUDE.md | ≤150 строк | Авто (всегда) |
| 3 | memory/*.md | ≤10 файлов, ≤100 строк | По триггерам |

| Контент | Куда |
|---------|------|
| Новый протокол | memory/protocol-*.md |
| Различение (1-3 строки) | CLAUDE.md (компактно) |
| Жёсткое различение | memory/hard-distinctions.md |
| Правило для типа репо | memory/repo-type-rules.md |
| Урок (кросс-системный) | memory/<topic>.md |
| Урок (одна система) | <repo>/CLAUDE.md |

> **CLAUDE.md НЕ в git.** `~/IWE/` — НЕ репозиторий. Версионирование — через backup в `DS-strategy/exocortex/`.
